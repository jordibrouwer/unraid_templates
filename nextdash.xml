<?xml version="1.0"?>
<Container version="2">
  <Name>NextDash</Name>
  <Repository>foobar1452/nextdash:latest</Repository>
  <Registry>https://hub.docker.com/r/foobar1452/nextdash</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/jordibrouwer/nextdash/issues</Support>
  <Project>https://www.nextdash.cc</Project>
  <ReadMe>https://raw.githubusercontent.com/jordibrouwer/nextdash/main/README.md</ReadMe>
  <TemplateURL>https://raw.githubusercontent.com/jordibrouwer/unraid_templates/main/nextdash.xml</TemplateURL>
  <Icon>https://github.com/jordibrouwer/nextdash/blob/main/static/nextdash-logo.png?raw=true</Icon>
  <ExtraParams>--restart unless-stopped</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DonateText>Find NextDash useful? Consider supporting the project.</DonateText>
  <DonateLink>https://github.com/sponsors/jordibrouwer</DonateLink>
  <Requires/>

  <Overview>
NextDash is a lightweight, self-hosted bookmark dashboard that features a minimalist, keyboard-driven interface with extensive customization options, making it the perfect personal dashboard for power users. Based on the excellent ThinkDashboard.

Key features:
• Keyboard-driven navigation for fast access to all your bookmarks
• Extensive theme and layout customization
• Inbox to collect new links fast: triage them, or keep them on a Kept tab and file whole groups later
• Health view that finds broken, stale and duplicate links, with uptime monitoring and downtime alerts
• Video bookmarks marked in the list, and played in the hover card without leaving the dashboard
• Widgets beside your bookmarks: 21 built-in kinds (uptime, feeds, weather, calendar, disks, containers and more) plus a Custom widget for any JSON API
• Six interface languages: English, Dutch, German, French, Spanish and Chinese
• Lightweight and fast, no heavy dependencies
• Actively maintained with regular releases

Links:
• Website: https://www.nextdash.cc
• Source code and releases: https://github.com/jordibrouwer/nextdash
• Changelog: https://github.com/jordibrouwer/nextdash/releases
• Devlog / release updates: https://www.nextdash.cc/news
• Also available on GHCR: ghcr.io/jordibrouwer/nextdash:latest
• User manual: https://github.com/jordibrouwer/nextdash/blob/main/MANUAL.md

The host widgets (Processor, Memory, Disks, Containers) are off until you fill in the optional paths and variables below — they are hidden under Show more settings. Section 11.4 of the manual explains what each one reads.

Support: open an issue on GitHub via the Support link above. Bug reports and feature requests are welcome.
  </Overview>

  <Changes>
### v1.13.5 (2026-09-24)
- Preview card: only a bookmark that is a video offers a play button — v1.13.4 drew one on every hover card

### v1.13.4 (2026-09-24)
- Bookmarks: a row whose link is a video carries a small play mark, read from the address alone
- Preview card: the thumbnail gains a play button, and the video plays in the card; nothing reaches the provider until it is pressed
- Keyboard: Shift+V puts the cursor on the play button; Escape, the close button or a click away ends the video
- Fixed: the byline and the player could not be kept in Config → Bookmarks; the keyboard cursor's glow was cut off square

### v1.13.3 (2026-09-23)
- Themes: every packaged theme names its character and the surfaces it was drawn for
- Appearance: Depth, Glow and Effects follow the theme until you change them, with one button back
- Theme browser: chips per character, search that reaches it, and previews at the theme's own surfaces

### v1.13.2 (2026-09-23)
- Fixed: reordering a page erased what its widgets watch, and a page could open without them
- Widgets: a tile catches the light the way a Config panel does
- Health: a certificate about to expire now reaches a webhook
- Unraid: the template carries the host widget mounts (Processor, Memory, Disks, Containers)
- Plus a long list of fixes across the inbox, previews, uploads and rate limits

### v1.13.1 (2026-09-22)
- Weather widget: fixed a blank tile — v1.13.0 asked Open-Meteo for the extra readings in a way that dropped the current conditions from the request

### v1.13.0 (2026-09-22)
- Widgets: a widget set to two columns now shows more, not the same figures larger — the load average behind the processor percentage, the container that is failing by name, used against total disk space, the expiry date of a certificate, what the weather feels like
- Widgets: row lists run in two columns on a wide tile, and narrow back to one on a one-column dashboard or a phone
- Weather: the forecast request now also carries apparent temperature, wind, humidity and chance of rain
- Kept widget: order by most recent, at random or by tag, with each link's address under its title

### v1.12.0 (2026-09-21)
- Inbox: Keep (Shift+K) moves a link to a new Kept tab with its note and tags, without filing it on a page
- Kept: group by site, tag or age, file a whole group at once, or work through links one at a time; rows suggest where a link belongs and which tags fit
- Undo: inbox, Kept, health and Config → Bookmarks changes can be taken back from their toast
- Previews and icons: fetching shows progress, waits out rate limits and can be stopped
- Logs: Record server log is a toggle in the log toolbar; every line carries a sequence number
- New Kept widget; What's new now also shows v1.11.1–v1.11.8
  </Changes>

  <Category>Tools: Productivity: Network:Web</Category>
  <WebUI>http://[IP]:[PORT:8080]</WebUI>
  <!-- Repository points to Docker Hub (foobar1452/nextdash) rather than GHCR
       so Unraid CA can show Last Update / download stats — GHCR doesn't
       expose the public registry metadata CA relies on for that. -->
  <Date>2026-09-24</Date>

  <Config Name="WebUI Port"
          Target="8080"
          Default="8103"
          Mode="tcp"
          Description="Port for the NextDash web interface"
          Type="Port"
          Display="always"
          Required="true"
          Mask="false" />

  <Config Name="AppData"
          Target="/app/data"
          Default="/mnt/user/appdata/nextdash/"
          Mode="rw"
          Description="Persistent storage path for NextDash data (bookmarks, settings)"
          Type="Path"
          Display="always"
          Required="true"
          Mask="false" />

  <!-- Everything below is optional and empty by default. Unraid skips a Path
       or Variable whose value is blank, so an install that wants none of this
       is unaffected. The pairs are from MANUAL.md section 11.4: each host
       widget needs its mount and the variable that points at it. -->

  <Config Name="Write token"
          Target="NEXTDASH_WRITE_TOKEN"
          Default=""
          Mode=""
          Description="Optional. Require this token in the X-NextDash-Token header on every write. Leave empty for an open instance on a trusted network."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="true" />

  <Config Name="Trusted proxies"
          Target="NEXTDASH_TRUSTED_PROXIES"
          Default=""
          Mode=""
          Description="Optional. Addresses and CIDR ranges whose X-Forwarded-For header is believed, comma-separated — set this if nextDash sits behind a reverse proxy, so rate limits count per visitor instead of per proxy. Left empty the header is ignored."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Config Name="Host proc"
          Target="/host/proc"
          Default=""
          Mode="ro"
          Description="Optional. Mount the host /proc here for the Processor and Memory widgets. Host path: /proc"
          Type="Path"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Config Name="Host proc variable"
          Target="NEXTDASH_HOST_PROC"
          Default=""
          Mode=""
          Description="Optional. Set to /host/proc once the mount above is filled in."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Config Name="Host disks"
          Target="/host/root/mnt"
          Default=""
          Mode="ro,rslave"
          Description="Optional. Mount the array here for the Disks widget. Host path: /mnt — then name /mnt/user or /mnt/cache in the widget."
          Type="Path"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Config Name="Host root variable"
          Target="NEXTDASH_HOST_ROOT"
          Default=""
          Mode=""
          Description="Optional. Set to /host/root once the disks mount above is filled in."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Config Name="Docker socket"
          Target="/var/run/docker.sock"
          Default=""
          Mode="ro"
          Description="Optional. Mount the Docker socket here for the Containers widget. Host path: /var/run/docker.sock — read-only still exposes the daemon's whole read API, so use a socket proxy if that is too much."
          Type="Path"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Config Name="Docker socket variable"
          Target="NEXTDASH_DOCKER_SOCKET"
          Default=""
          Mode=""
          Description="Optional. Set to /var/run/docker.sock once the socket mount above is filled in. Unset, the Containers widget is hidden."
          Type="Variable"
          Display="advanced"
          Required="false"
          Mask="false" />

  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-dashboard.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-bookmarks.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-search.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-inbox.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-health.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-health-monitors.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-themes.jpg</Screenshot>
  <Screenshot>https://github.com/jordibrouwer/nextdash/raw/main/screenshots/nextdash-config.jpg</Screenshot>
</Container>
